{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker run",
            "type": "shell",
            "command": "docker build -t demos-devops ."
        },
        {
            "label": "docker run",
            "type": "shell",
            "command": "docker run --rm demos-devops"
        }
    ]
}